# Task: Show user's own complaints in mycomplaints.html

## Steps:
- [x] 1. Analyze project files and confirm plan (missing /api/mycomplaints/:username in backend.js)
- [x] 2. Edit backend.js to add GET /api/mycomplaints/:username endpoint (filter complaints by username)

- [x] 3. Verify no changes needed to mycomplaints.html (already fetches user-specific API)

- [x] 4. Instruct user to restart server and test (login → mycomplaints.html → see only own complaints)

- [x] 5. Complete task

